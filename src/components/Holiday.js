import {Box, Typography} from '@material-ui/core'


const Holiday = () => {
    return (
        <Box margin="5%">
            <Typography variant="h3">this is where you request a kounji</Typography>
            <Typography variant="p">don't over do it 😉 </Typography>
        </Box>
    )
}

export default Holiday
